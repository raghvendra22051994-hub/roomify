# Example env for local development — copy to `.env.local` and fill in real values.
# DO NOT commit `.env.local` (it's excluded by .gitignore).
# VITE_PUTER_WORKER_URL: URL used by the client to reach the Puter worker service.
# Example placeholder value — replace with your real worker URL when running locally.
VITE_PUTER_WORKER_URL=https://your-puter-worker.example
